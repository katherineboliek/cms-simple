<!DOCTYPE html>
<html>
    <head>
        <title>Blog - Posts</title>
        <link href='https://fonts.googleapis.com/css?family=Source+Sans+Pro:400,700,400italic|Source+Serif+Pro' rel='stylesheet' type='text/css'>
    </head>
    <body>
        <h1>Katherine's Blog</h1>
        <% if (posts.length) { %>
            <% for (var i in posts) { %>
            <h2><a href="/posts/<%= posts[i].custom %>"><%= posts[i].title %></a></h2>
            <% } %>
        <% } else { %>
        <p>Add a post to begin.</p>
        <% } %>

        <p><a href="/post/new">New Post</a></p>
    </body>
</html>
